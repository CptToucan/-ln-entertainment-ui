<span class="text-white text-xl px-2">
  Pick a game:
</span>

<div class="grid grid-cols-2 gap-4 px-2 mt-4">
  {{#each this.games as |game|}}
    <button
      class="shadow-md border-solid border-2 border-red-500 text-white px-4 py-5 rounded hover:bg-red-500 chalk-text font-semibold"
      {{on "click" (fn @gameSelected game)}}
    >
      {{game.displayName}}
    </button>
  {{/each}}
</div>